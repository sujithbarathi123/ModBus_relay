# ModBus_relay
To control a relay with the Modbus protocol, To use a master device (QmodMaster) to send specific data frames to a slave device (the relay module) over a communication interface like RS-485 (RTU).
